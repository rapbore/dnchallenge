<p id="notice"><%= notice %></p>

<h1>Listing Colegios</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @colegios.each do |colegio| %>
      <tr>
        <td><%= colegio.nombre %></td>
        <td><strong><%= link_to 'Send Message', new_message_colegio_path(colegio) %></strong></td>
     </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Colegio', new_colegio_path %>
